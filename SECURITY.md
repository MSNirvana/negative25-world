# Security Policy

## 支持版本

只对 main 分支和最近的生产部署版本提供安全修复支持。

## 报告安全问题

不要在公开 Issue 中发布密码、Token、预签名 URL、数据库连接串、原图地址或可利用步骤。请通过仓库的 GitHub Security Advisory 私下报告，并尽量提供：

- 受影响的版本或提交；
- 可复现步骤和影响范围；
- 脱敏后的日志、请求和响应；
- 你建议的修复方向（如果有）。

收到报告后，维护者会确认问题、评估影响并在修复后公开说明。请给用户数据和第三方服务留下足够的撤销/轮换时间。

## 部署安全基线

- 使用长随机 JWT_SECRET 和 JWT_REFRESH_SECRET，并定期轮换；
- 原图 bucket 保持私有，公开只读策略仅覆盖生成的预览变体；
- 浏览器预签名地址使用公网 S3_PUBLIC_ENDPOINT，不要暴露 Docker 内部主机名；
- 限制高德 Key 的域名和 API 范围；
- PostgreSQL、Redis、MinIO Console 只绑定内网或回环地址；
- 对公开主页、上传、导入和搜索接口设置反滥用策略与日志告警。
